const copyArray = function (arr) {
    const buffer = [];
    
    foreach(const el in arr) {
        buffer.add(el);
    }
    
    return buffer;
};

const arr = [1, 2, 3];
const copy = copyArray(arr);
console.log(arr);
console.log(copy);